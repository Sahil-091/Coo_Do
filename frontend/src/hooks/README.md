# hooks

Shared custom hooks live here once more than one feature needs them.
Feature-specific hooks should stay co-located inside their own
`features/<name>/` folder instead of living here by default.
